// Re-export shared types — single source of truth
export type {
  Profile,
  LoyaltyTier,
  Bonus,
  Document,
} from '@shineshop/types';
export type { NotificationFeedItem } from '@shineshop/types';
