// Re-export shared types — single source of truth
export type {
  Product,
  ProductStatus,
  ProductListItem,
  Category,
  Brand,
  CatalogFilters,
  SortOption,
} from '@shineshop/types';
export { SORT_OPTIONS } from '@shineshop/types';
