// Re-export shared types — single source of truth
export type {
  Order,
  OrderStatus,
  OrderSource,
  OrderItem,
  OrderContact,
  OrderShippingAddress,
  ShippingMethod,
  PaymentMethod,
} from '@shineshop/types';
export { ORDER_STATUS_MAP } from '@shineshop/types';
